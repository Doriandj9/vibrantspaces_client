
type EditorTextProps = {
    status: string;
    editor: unknown;
};

export const EditorText: React.FC<EditorTextProps> = ({editor, status}) => {

    console.log(editor,status);
    return (
        <>
        
        </>
    );
};